# EMERGENCY: Pre-built wheels guaranteed for Python 3.14 on Linux
# These versions have .whl files available - NO compilation needed!

# Core (pre-built wheels available)
fastapi==0.100.0
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Database
sqlalchemy==2.0.0
alembic==1.13.0
aiosqlite==0.19.0

# Pydantic 2.0.0 has pre-built wheels for Python 3.14
# DO NOT use newer versions (2.5+) - they require Rust compilation
pydantic==2.0.0

# Utilities (all have pre-built wheels)
beautifulsoup4==4.12.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0
aiofiles==23.2.0
json5==0.9.0

# Note: If this still fails, try installing pydantic separately:
# pip install --only-binary :all: pydantic==2.0.0
# Then: pip install --only-binary :all: -r requirements-prod.txt
