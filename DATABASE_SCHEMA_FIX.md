#!/usr/bin/env bash
# ⚠️ CRITICAL: Delete old database and recreate with proper schema

echo "🔧 DATABASE SCHEMA FIX - Required Steps:"
echo "========================================"
echo ""
echo "The database on Pterodactyl is missing the 'title' and 'chapter' columns."
echo "This guide will help you fix it."
echo ""

echo "✅ OPTION 1: RECOMMENDED - Delete old database (Clean restart)"
echo "==========================================================="
echo "1. Stop the server on Pterodactyl (click Stop button)"
echo ""
echo "2. In Pterodactyl File Manager, DELETE this file:"
echo "   /home/container/flashcard_hub.db"
echo ""
echo "3. Sync latest code:"
echo "   cd /home/container"
echo "   git reset --hard && git pull origin main"
echo ""
echo "4. RESTART the server (click Start button)"
echo ""
echo "5. The server will:"
echo "   - Detect missing database"
echo "   - Run migrations automatically"
echo "   - Create fresh schema with all columns ✅"
echo ""

echo "If you see these logs, it worked:"
echo "   ✅ Database migrations completed"
echo "   ✅ Card columns verified"
echo "   ✅ Frontend mounted successfully at /"
echo ""

echo "========================================"
echo "✅ OPTION 2: Keep existing data"
echo "==========================================================="
echo "If you want to keep existing decks/cards:"
echo ""
echo "1. DON'T delete the database"
echo "2. Just restart with new code (commit d209552)"
echo "3. The server will auto-detect missing columns and ADD them"
echo ""
echo "Watch logs for:"
echo "   ⚠️ Adding missing 'title' column to cards table"
echo "   ⚠️ Adding missing 'chapter' column to cards table"
echo ""

echo "========================================"
echo "❌ IF STILL FAILING AFTER RESTART:"
echo "==========================================================="
echo "If you STILL see 'no such column' errors:"
echo ""
echo "1. Run this in Pterodactyl console to delete database:"
echo "   rm -f /home/container/flashcard_hub.db"
echo ""
echo "2. Restart the server"
echo ""
echo "3. The fresh database will have all columns ✅"
echo ""
